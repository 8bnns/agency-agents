# Not An Agent

This file has no YAML frontmatter at all. It represents the non-agent docs
(QUICKSTART.md, strategy/ playbooks, etc.) that the tooling must silently skip
in convert.sh/install.sh and flag as an ERROR in lint-agents.sh.
